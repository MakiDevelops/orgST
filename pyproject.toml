[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "orgST"
readme = "readme.md"
authors = [
    {name = "Wdboyes13"},
    {name = "MakiDevelops"}
]
version = "1.0"
description = "orgST remade!"
requires-python = ">=3.8"
license = "MIT"
dependencies = [
    "GitPython>=3.1.0"
]

[project.urls]
Homepage = "https://github.com/MakiDevelops/orgST"
